set -g default-terminal "xterm-256color"

set -ga terminal-overrides ",xterm-256color*:Tc"
set-option -g default-shell /usr/bin/zsh
#set -g default-terminal "screen-256color"


#Send prefix
unbind C-b
set-option -g prefix M-a
bind-key M-a send-prefix


#Set easier panes split keys
bind-key l split-window -h
bind-key j split-window -v

# Use Alt-vim keys without prefix key to switch panes
#bind -n C-M-h select-pane -L
#bind -n C-M-j select-pane -D 
#bind -n C-M-k select-pane -U
#bind -n C-M-l select-pane -R

bind -n C-M-h select-pane -L
bind -n C-M-j select-pane -D 
bind -n C-M-k select-pane -U
bind -n C-M-l select-pane -R

bind -n M-J resize-pane -D 5
bind -n M-K resize-pane -U 5
bind -n M-H resize-pane -L 5
bind -n M-L resize-pane -R 5

#Dontn't prompt to kill panes/windows.
bind-key k kill-pane

 
#Alt+w to kill windows
bind-key K kill-window

# Disable escape time
set -sg escape-time 0

#Mouse mode
setw -g mouse on

#Easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded."

#Start window numbering at 1
set -g allow-rename off    # stop renaming windows automatically
set -g base-index 1        # start window numbering at 1
set -g pane-base-index 1   # keep pane numbering consistent with windows
set-window-option -g allow-rename off
set -g renumber-windows on # renumber windows when window closed




############################
## Status Bar
############################

# enable UTF-8 support in status bar
set -gq status-utf8 on

# set refresh interval for status bar
set -g status-interval 60

# center the status bar
set -g status-justify centre

# show session, window, pane in left status bar
set -g status-left-length 40
set -g status-left '#[fg=white] #S #[fg=white]#P/#[fg=white]#I '

# show hostname, date, tim 100
set -g status-right ' #[fg=white]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white] %d %b %R'
###########################
# Colors
###########################
# color status bar
set -g status-style fg=white,bg=colour235
# color of message bar
set -g message-style fg=white,bold,bg=green

# highlight current window
setw -g window-status-style fg=default,dim,bg=colour235

setw -g window-status-current-style fg=white,bright,bg=default

setw -g window-style fg=colour250,bg=default
setw -g window-active-style fg=colour255,bg=default

# dark gray for split borders
set -g pane-active-border-style bg=black,fg=colour246
set -g pane-border-style fg=colour235 


# Set the default terminal terminfo
#set -g default-terminal "tmux-256color"
# True colour support
# Update the status line every sixty seconds
#set -g default-terminal "alacritty"

# Smart pane resizing with awareness of Vim splits.
# See: https://github.com/RyanMillerC/better-vim-tmux-resizer
#is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|vim?x?)(diff)?$'"

# Edit values if you use custom resize_count variables
#bind-key -n M-C-H if-shell "$is_vim" "send-keys M-h"  "resize-pane -L 10"
#bind-key -n M-C-J if-shell "$is_vim" "send-keys M-j"  "resize-pane -D 5"
#bind-key -n M-C-K if-shell "$is_vim" "send-keys M-k"  "resize-pane -U 5"
#bind-key -n M-C-L if-shell "$is_vim" "send-keys M-l"  "resize-pane -R 10"

#bind-key -T copy-mode-vi H resize-pane -L 5
#bind-key -T copy-mode-vi J resize-pane -D 5
#bind-key -T copy-mode-vi K resize-pane -U 5
#bind-key -T copy-mode-vi L resize-pane -R 5


set -g @plugin 'tmux-plugins/tpm'
#set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
run '~/.tmux/plugins/tpm/tpm'
